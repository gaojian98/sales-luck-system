# Frontend Manual QA Script

## 0) 启动

1. 启动后端：`cd backend && npm run dev`
2. 启动前端：`cd frontend && npm run dev`
3. 打开：`http://127.0.0.1:5173/`

## 1) 首屏检查

1. 页面正常渲染，无白屏。
2. 右上区域显示“未登录”。
3. 钱包区域显示积分/能量（未登录时可为 `--`）。

## 2) 注册流程（按钮级）

1. 点击右上 `注册` 按钮。
2. 在弹窗中至少填写：
   - 姓名
   - 登录账号
   - 登录密码
   - 联系方式（邮箱或手机号）
3. 点击 `提交注册`。
4. 预期结果：
   - 弹出“注册成功，请登录”提示
   - 注册弹窗关闭，登录弹窗打开
   - 登录表单已自动回填账号和密码

## 3) 登录流程（按钮级）

1. 在登录弹窗确认账号密码。
2. 点击 `确认登录`。
3. 预期结果：
   - 弹窗关闭
   - 页面右上显示当前用户名
   - 钱包区域显示积分与能量

## 4) 抽奖流程（按钮级）

1. 点击转盘中心 `开始`。
2. 等待转盘动画结束。
3. 预期结果：
   - 出现结果弹窗
   - 钱包积分发生变化（按后端返回）
   - 如果有能量，能量可能减少
   - “最近抽奖”出现新记录

## 5) 低分后的充值流程（按钮级）

1. 在结果弹窗中点击 `继续加持`（低分时应打开充值弹窗）。
2. 在充值弹窗点击任意套餐按钮：
   - `体验包 8.8（+10 能量）`
   - `推荐包 58.8（+80 能量）`
   - `高能包 188（+300 能量）`
3. 预期结果：
   - 弹出充值成功提示
   - 充值弹窗关闭
   - 能量余额增加

## 6) 会话与退出

1. 刷新页面。
2. 预期结果：
   - 仍保持登录态
   - 钱包数据可重新加载
   - 最近抽奖记录仍可展示
3. 点击 `退出`。
4. 预期结果：
   - 当前用户回到“未登录”
   - 钱包与记录展示清空

## 7) 异常检查

1. 不登录直接点转盘，预期提示“请先登录”并打开登录弹窗。
2. 登录时输入错误密码，预期提示“密码错误”。
3. 积分不足时抽奖，预期提示“积分不足”。
